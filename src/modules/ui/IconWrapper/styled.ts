import styled from 'styled-components';

export const StyledIconWrapper = styled.span`
  display: inline-flex;
  align-items: center;
  justify-content: center;
  font-size: var(--icon-f-size);
  color: var(--icon-color);
  line-height: 1;
`;
