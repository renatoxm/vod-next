import styled from 'styled-components';
import IconButton from '@/modules/ui/IconButton';

export const StyledVoiceSearch = styled.div`
  flex: 1;
  position: relative;
`;

export const StyledMicroButton = styled(IconButton)`
  position: absolute !important;
  top: 0.35em;
  right: 1em;
`;
